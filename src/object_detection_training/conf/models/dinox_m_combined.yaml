defaults:
  - dinox_m_baseline
  - _self_

# E: DFL + Soft Labels + MAL combined (no TAL, no dual head)
use_dfl: true
reg_max: 16
dfl_loss_weight: 0.25
use_soft_labels: true
soft_label_gamma: 2.0
use_log_iou_cost: true
use_mal: true
mal_gamma: 1.5
